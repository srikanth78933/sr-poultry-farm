# URL where the browser reaches the backend API.
# Local dev (no nginx):   http://localhost:8000
# Behind nginx (compose): leave empty to use same-origin "/api"
NEXT_PUBLIC_API_URL=http://localhost:8000
